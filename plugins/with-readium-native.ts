// @ts-nocheck
import { ConfigPlugin, withDangerousMod } from '@expo/config-plugins';

// Minimal placeholder config plugin to add native dependencies later
const withReadiumNative: ConfigPlugin = (config) => {
  return config;
};

export default withReadiumNative;

